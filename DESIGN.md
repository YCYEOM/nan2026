# Product design identity — NAN2026 Harness

이 문서는 디자인 의도의 단일 명세다. 값은 `src/ui/tokens.ts` 에 있고 여기는 의미와 사용 원칙만 정의한다.

## Signature

**네온 핑크 `#FF2E97` → 사이언 `#00E5FF`**, 보랏빛 흑(`#0A0612`) 위.

이 그라데이션은 **정보를 전달하지 않는 장식에만** 쓴다 — 타이틀, 레인 테두리, 캔버스
외곽 글로우. 판정선·노트처럼 상태를 알리는 요소에는 쓰지 않는다.

이유: 그라데이션 끝 `#00E5FF` 가 P1 색과 같은 값이다. 상태 요소에 쓰면 원칙 1이
깨지고 "지금 P1 차례인가?"가 모호해진다. 판정선은 **현재 담당 플레이어 색**으로
빛나게 해서, 화면에서 가장 큰 시각 요소가 차례를 말하도록 했다.

`C.accent` 는 시그니처 핑크와 같은 값이지만 역할이 다르다(주목: 콤보·번호·hover).
값이 같다고 토큰을 합치지 않는다 — 나중에 갈라질 수 있다.

각 항목은 코드 조사 결과에 따라 표시한다:
`CONFIRMED` 코드가 이미 일관되게 따름 / `INCONSISTENT` 코드가 어긋남(정리 대상) /
`PROPOSED` 이번에 새로 정한 규칙.

## Purpose

로컬 핫시트 협동·대결 게임을 **캔버스 한 장(640×480)** 에 그린다. 관객이 옆에서 보는
시연 상황이라, 디자인은 "지금 누구 차례고 방금 무슨 일이 일어났는가"를 설명 없이
읽히게 만드는 것을 최우선한다. 미적 완성도보다 판독성이 먼저다.

## Design principles

1. **플레이어 색은 절대 재사용하지 않는다.** P1 사이언·P2 앰버는 그 사람을 가리킬 때만 쓴다.
   상태(성공/실패)나 장식에 쓰면 "누구 것인가"가 무너진다.
2. **상태는 색 하나로만 구분하지 않는다.** 색 + 위치·크기·텍스트를 함께 바꾼다.
   시연 화면과 색각 이상 모두에서 읽혀야 한다.
3. **판정 결과는 0.5초 안에 사라진다.** 화면에 누적되는 정보는 점수·생명·차례뿐이다.
4. **숫자 옆에는 항상 모수를 적는다.** `92%` 가 아니라 `92%(18노트)`.
   비율만 보여주면 표본이 작은 쪽이 유리해 보인다.
5. **장식 효과는 판정을 가리지 않는다.** 파티클·흔들림은 판정선과 노트 위에 겹치지 않는다.
6. **새 색이 필요하면 먼저 여기에 역할을 적는다.** 씬에 리터럴을 직접 쓰지 않는다.

### 원칙 1의 유일한 예외 — 반전 노트 `PROPOSED`

반전 노트(`swapOf(k)`)는 **P1 의 색과 줄에서 등장하지만 P2가 눌러야 한다.** 색과 위치가
담당을 말하지 않는 유일한 경우다. 게임 기믹이므로 원칙을 조용히 깨는 대신 예외로 못 박는다.

**색만 거짓말시켜서는 부족하다.** 레인이 생긴 뒤(TASK-007) 위치가 색보다 강한 신호가 됐다.
담당 레인에 그리면 "저 사람 줄에 있으니 저 사람 것"으로 반전이 장식이 된다(플레이테스트
지적: "라인으로 오니까 그 사람이 누르면 그냥 맞게 되는 거 아니야?"). 그래서 등장 위치도
색과 같이 거짓말한다 — 노트는 **상대 레인에서 등장해 담당 레인으로 건너온다**(`laneAt`).

이 예외가 성립하는 조건은 **다른 네 신호가 사실을 말한다**는 것이다.

1. **판정선 표적 원**은 담당 레인에서 빛난다. 노트가 아직 남의 줄에 있어도 표적은 진실이다.
2. **노트 안의 키 글자**가 눌러야 하는 키다. 남의 줄에 있는 노트가 내 키가 아닌 글자를
   달고 있다 — 이 불일치 자체가 반전의 신호다.
3. **담당 색 점선 링**이 노트를 두른다(`rad + 6`, 파선 5/4). 한 노트에 두 색이 겹치므로
   "반전 노트다"가 멀리서 보이고, 점선 색이 눌러야 하는 사람을 말한다.
4. **차례 안내**가 문장으로 명시한다 — `🔀 반전! P1 색 → P2 가 [K]`.

즉 등장 시점의 색·위치만 속이고 판단 정보는 전부 준다. 반사는 색과 줄을 따라가므로
난이도가 유지되고, 판정 시점에는 담당 레인으로 수렴해 표적 원과 어긋나지 않는다 —
"어디를 쳐야 하는지 모르겠다"가 아니라 "어? 내 게 아니었네"가 된다.

**반전 표식을 글자에 걸지 않는다.** 처음엔 노트 안에 `🔀P2` 를 적었는데 반지름 18~22
원 안의 12px 이모지는 뭉개져서 순간 판독이 되지 않았다(플레이테스트에서 "가시성이 좋지
않다"). 원칙 2가 말하는 대로 표식은 **모양**으로 준다 — 점선 링은 색·굵기·파선이 동시에
다르므로 색각 이상과 시연 화면 모두에서 읽힌다. 이모지는 큰 글씨(F.xl 차례 안내)에서만 쓴다.

반전 비율은 스테이지 2부터 15%, 스테이지당 +7%, 상한 35%다. 상한을 두는 이유는
반전이 절반을 넘으면 색 체계 자체가 의미를 잃기 때문이다.

## Color palette

역할 기반. 실값은 `src/ui/tokens.ts`.

| 역할 | 토큰 | 값 | bg 대비 | 용도 |
|---|---|---|---:|---|
| 시그니처 | `C.sig` / `C.sigEnd` | #FF2E97 / #00E5FF | 5.81 / 13.03 | 타이틀·레인 테두리·외곽 글로우 (장식 전용) |
| 바탕 | `C.bg` | #0A0612 | — | 화면 전체 배경 |
| 판 | `C.surface` / `C.surfaceAlt` / `C.surfaceHi` | #150E24 / #1D1330 / #2A1B45 | — | 레인, 메뉴 행, hover |
| 비활성 | `C.slot` | #241A38 | — | 꺼진 슬롯, 내 차례 아닌 표시 |
| 선 | `C.line` | #3D2A5C | — | 테두리·구분 |
| 본문 | `C.text` | #FFFFFF | 20.04 | 주 텍스트 |
| 보조 | `C.textMuted` | #B9A8D4 | 9.18 | 설명·부연 |
| 흐림 | `C.textFaint` | #7A6A96 | 4.13 | 비활성·부가. **작은 텍스트 AA 미달** — 판단에 필요한 정보에 쓰지 않는다 |
| 주목 | `C.accent` / `C.accentHi` | #FF2E97 / #FFD166 | 5.81 / 13.6 | 콤보, 번호, hover, 연타 링 |
| 성공 | `C.success` | #3DFF9E | 15.27 | 클리어·달성·켜짐 |
| 실패 | `C.danger` | #FF5252 | 6.28 | 미스·정전·패배 |
| 외부 수요 | `C.demand` | #C77DFF | 7.45 | 전력망 수요 미터 |
| 오버레이 | `C.scrim` | #000000 | — | 항상 `withAlpha` 와 함께 |
| 플레이어 | `C.player[i]` | P1 #00E5FF / P2 #FFB300 | 13.03 / 11.17 | i번 플레이어 전용 |

대비는 WCAG 2.1 상대휘도로 실측한 값이다(`C.bg` 기준).

`C.player` 는 **2개만 정의한다.** P3·P4 를 추가할 때는 `C.success`·`C.danger`·`C.accent`·
`C.demand` 어느 것과도 겹치지 않고, 기존 플레이어 색과 밝기가 달라야 한다(원칙 2).
미리 정해두면 반드시 충돌한다 — 실제로 이전 팔레트의 P3 는 `C.success` 와 같은 값이었다.

`C.danger`(#FF5252) 와 시그니처 핑크(#FF2E97) 는 상호대비 1.08 로 밝기가 비슷하다.
MISS 는 항상 텍스트와 화면 흔들림을 동반하므로 색만으로 콤보와 구분하지 않는다(원칙 2).

## Typography

`F.xs 12 / sm 14 / md 16 / lg 18 / xl 22 / xxl 28 / huge 34 / hero 44`, 전부 `sans-serif`.

- `hero` 결과 화면의 정확도 한 줄 · `huge` 결과 제목 · `xxl` 판정 팝업
- `xl` 콤보·메뉴 번호 · `lg` 씬 내 주요 라벨 · `md` 일반 · `sm` 설명 · `xs` 조밀한 라벨
- 단계 사이 값(13, 15, 17, 19, 20, 24, 30, 40)은 쓰지 않는다.

`INCONSISTENT` — 정리 전 17단계(11~44px)가 위계 없이 쓰이고 있었다.

## Layout

캔버스 640×480 고정. 반응형 없음 — 시연 장비가 정해져 있고 본선까지 바꿀 일이 없다.
HUD만 캔버스 밖 DOM(`#hud`)이고 좌상단 고정.

## Interaction states

캔버스 게임이라 hover/focus 개념이 있는 곳은 메뉴뿐이다.

- 메뉴 행: 기본 `C.surfaceAlt` + `C.line` 테두리 / hover `C.surfaceHi` + `C.accent` 테두리
- 게임 중: 내 차례 `C.player[i]`, 아니면 `C.slot`
- active(누르는 중): 홀드·연타는 눌린 동안 노트가 남은 양을 계속 갱신한다.
  메뉴 행에는 active 표현이 없다 — 클릭 즉시 씬이 바뀌어 보일 틈이 없다.
- disabled(쿨다운): `C.textFaint` + 남은 초 표기
- error: 플레이 중 오류는 미스 판정으로만 나타난다(`C.danger` + `MISS` + 흔들림).
  치명 오류 화면은 없다 — 캔버스가 죽으면 `R` 로 재시작한다.
- success: 판정 성공은 `PERFECT!`/`GOOD` 팝업(`C.success`/`C.accent`) + 충격파 링 +
  파티클 버스트 + 화면 흔들림으로 알린다. 등급별 수치는 Motion 절 참조.
  스테이지 클리어는 종료 화면 제목과 정확도를 `C.success` 글로우로 띄운다.
  색만으로 알리지 않는다(원칙 2) — 항상 텍스트가 함께 나온다.
- loading / empty 상태 없음 — 에셋을 로드하지 않고 빈 화면이 나오는 경로가 없다

`focus` 는 미구현이다. 키보드로만 조작하는 게임이라 포커스 링이 필요한 요소가 없지만,
메뉴는 마우스·숫자키 둘 다 받으므로 키보드 이동 시 현재 항목 표시가 없다. `MISSING`

## Motion

- 판정 팝업 0.5초 페이드. 등급별로 크기와 방향이 다르다 — perfect 는 `F.xxl` 로 위로
  튀어오르고 글로우, good 은 `F.xl` 로 살짝 위로, miss 는 `F.xl` 로 아래로 처진다.
  위치는 판정선 **왼쪽**(x=75), 판정된 레인 높이다. 노트는 `x >= HIT_X` 에만 있어
  이 구역은 구조적으로 비어 있다 — 원칙 5를 규칙이 아니라 좌표로 보장한다.
- 판정 피드백은 등급이 한 프레임만 봐도 갈려야 한다(`feedback()` 한 곳에 모여 있다):
  충격파 링 perfect 2겹 / good 1겹 / miss 없음, 파티클 22·9·7,
  화면 흔들림 3.5·1·8, 붉은 가장자리는 miss 에만.
- 파티클 버스트는 히트 지점에서만, 중력 감쇠
- `prefers-reduced-motion` 미대응 `MISSING`

## Voice and microcopy

- 한국어. 격식체 대신 짧은 평서·감탄형(`스테이지 실패`, `🏆 P1 승`).
- 지시문은 무엇을 누르는지 먼저: `⚡ P1 연타! [D] ×3`
- 실패 문구는 원인을 밝힌다: `리듬 붕괴 — 생명 소진`, `정전 도시 — 급전 실패`
- 이모지는 상태 아이콘으로만. 문장 장식으로 쓰지 않는다.

## Accessibility

- 색만으로 구분하지 않는다(원칙 2). 플레이어 표시는 색 + `P1`/`P2` 텍스트를 함께 쓴다.
- 레인은 색만으로 나누지 않는다. 소유자 색 5% 바탕 + 구분선 굵기(플레이어 경계는 진하게,
  같은 사람 키 사이는 흐리게) + 노트 안의 **키 글자** 세 채널을 함께 쓴다.
  키가 여럿이면 `P1` 라벨은 정보가 아니다 — 누를 키 자체를 적는다.
- 실측 대비는 색 역할표에 적어뒀다. `C.textFaint`(4.13)만 작은 텍스트 AA(4.5) 미달이다.
  보조 정보에만 쓰고 판단에 필요한 정보는 `C.textMuted`(9.18) 이상을 쓴다.
- 음소거 `M` 제공. 소리 없이도 플레이 가능해야 한다 — 청각 단서는 전부 시각 단서와 짝을 이룬다.

## Do

- 새 화면은 `C`·`F` 만 참조해 그린다
- 비율을 보여줄 때 모수를 함께 적는다
- 실패 화면에 원인과 다음 행동을 같이 적는다

## Do not

- 플레이어 색을 상태·장식에 쓰지 않는다
- 색 리터럴·폰트 크기 리터럴을 씬에 직접 쓰지 않는다
- `rgb()`/`rgba()` 리터럴을 쓰지 않는다. 알파가 필요하면 `withAlpha(토큰, a)` 를 쓴다.
  `bass design check` 는 hex 만 검사하므로 이 규칙은 기계가 잡아주지 않는다
- 시그니처 그라데이션을 상태·정보 전달 요소에 쓰지 않는다
- 타이포 단계 사이 값을 만들지 않는다
- 판정에 필요한 요소 위에 이펙트를 겹치지 않는다

## Decisions and history

- 값 정리 경위와 남은 불일치는 `docs/DECISIONS.md` 참조.
