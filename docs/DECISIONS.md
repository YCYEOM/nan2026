# DECISIONS

> 결정 로그 + 폐기 기록(문서 4·8). AI 결과를 사람이 비교·통제했다는 증거.

## 채택

| 시각 | 결정 | 이유 | 관련 커밋 |
|---|---|---|---|
| 07-27 | 리허설2 주제 "전염" 채택. 게임 = **봉쇄(Containment)** | 20개 → 게이트 6통과 → 채점 1위(88). 기존 Grid 재사용 + 신규 1개(Infection). chainReact와 달리 *시간차* 확산이라 신규 필요 | src/scenes/contagion.ts |
| 07-27 | 리허설3 주제 "전력망" 채택 (협동+트롤). 온라인 대신 **핫시트 2인** | 컨셉 3속성(같은행동=도움·피해 / 가려짐 / 공동운명)을 지연미터·공유용량으로 구현. 신규 1개(PowerGrid) | src/scenes/power.ts |
| 07-27 | 고도화 A·B·C + 브라운아웃 얹음 | A=개인MVP(공유지의비극) / 브라운아웃=과욕 외부효과 / C=비대칭역할+애매한책임로그 / B=수요급증+열피드백 | src/systems/powergrid.ts |
| 07-27 | 크로서 페널티: 라인 먼저 넘긴 사람 −100 (환경 탓이면 무적용) + 과부하 알람 텔레그래프 | 넘긴 직후 grace 동안만 경고(넘기기 전 무경고=발뺌 유지). 브링크맨십 유발 | src/systems/powergrid.ts, src/scenes/power.ts |
| 07-27 | **경쟁 과열 → 팀웍 강화**: 시너지×1.3 + 무정전 스트릭(→×1.5, 정전 시 리셋) + MVP는 팀 승리 시에만 | 개인MVP·−100이 라이벌 구도를 만듦. 협동이 *수치로 이기게* 해 트롤을 '가능하나 값비싼 선택'으로 되돌림. 컨셉(협동 기본) 복원 | src/systems/powergrid.ts (TEAM) |
| 07-27 | **스펙트럼 확장**: 기계 5종(+초효율·불안정) + 랜덤 이벤트 3종(정전폭풍·수요쇼크·정비) | 매 판 변주·전략 폭↑. 시드 RNG로 앱은 랜덤·테스트는 결정적. seed/volatile 없으면 비활성 → 코어 테스트 무손상. 불안정 스파이크 = 새 발뺌 트롤 | src/systems/powergrid.ts, src/scenes/power.ts |
| 07-27 | **목표 강화**: 진행 바 → 도시 구역 10개 순차 점등(도시 공급 서사) | "왜 생산하나" 가시화 + 정전 시 구역이 꺼져 공동 스테이크↑. produced 파생이라 모델·테스트 무변경 | src/scenes/power.ts |
| 07-27 | **라운드 후 정산**: 정전 이력(시각·범인·초과기여)을 종료 화면 타임라인으로 공개 | 판 내내 애매했던 '누구 탓'을 끝에서 해소 = 가려짐의 페이오프. 환경 탓 정전은 정직히 표기 | src/systems/powergrid.ts (history), src/scenes/power.ts |
| 07-27 | **오디오 juice**: Web Audio 신스 효과음(토글·역할·급등·이벤트·경고·트립·승패) + 음소거 토글(키 M) | 에셋 없이 긴장감·피드백↑. 감각 계층이라 모델·테스트 무변경 | src/systems/sfx.ts, src/scenes/power.ts |
| 07-27 | **온도·전염 리허설 제거** — 씬·HeatField·Infection 시스템·해당 테스트 삭제, 전력망만 유지 | 사용자 요청. 재사용 키트(Grid/DragDrop/behaviors)는 하네스로 존치. 테스트 30→22 | src/scenes, src/systems |
| 07-27 | **리허설4 "리듬 릴레이"** 추가 (협동, 2인→N인 확장형): 돌아가며 타이밍 버튼, 팀 콤보·생명 공유 | 새 주제. 신규 1개(RhythmEngine). PLAYERS만 늘리면 N인. Sfx 재사용 | src/systems/rhythm.ts, src/scenes/rhythm.ts |
| 07-27 | **씬 선택 메뉴** 추가 — 시작 화면에서 게임 선택, Esc로 복귀. GAMES 배열로 등록 | 게임이 둘 이상 → 한 빌드에서 선택 플레이. 하네스 씬 교체 설계 실증. 전력망·리듬 유지(온도·전염만 제거됨) | src/scenes/menu.ts, src/main.ts, src/core/harness.ts |
| 07-27 | **리듬 심화: 동시 비트(합주)** — 4번째마다 전원이 함께 눌러야 성공(보너스×2) | 라운드로빈은 협동이 얕음(각자 제 몫). 동시 비트가 '같이 맞추는 순간'을 만들어 협동 강제 | src/systems/rhythm.ts, src/scenes/rhythm.ts |
| 07-27 | **리듬 모드: 팀전/개인전** — team(공유 콤보·생명+동시)·solo(각자 점수 레이스, 동시 없음) | 협력·경쟁 둘 다 지원. 메뉴 항목 2개. 씬 `new RhythmScene(h, mode)` | src/systems/rhythm.ts, src/scenes/rhythm.ts, src/main.ts |
| 07-27 | **리듬 변칙 패턴** — seed 있으면 담당 순서·비트 간격 불규칙(LCG). seed 없으면 균일(테스트 보존) | 균일 라운드로빈은 예측 가능·단조 → 불규칙 담당·간격으로 긴장·집중 | src/systems/rhythm.ts, src/scenes/rhythm.ts |
| 07-27 | **리듬 롱노트(홀드)** — 누르고 유지하다 끝에 뗌, 길수록 보너스. 일찍 뗴면 미스 | 탭만 있던 조작에 길이 축 추가. keydown=press/keyup=release. 변칙 차트 25%가 홀드 | src/systems/rhythm.ts, src/scenes/rhythm.ts |
| 07-27 | **리듬 멜로디(BGM)** — 직접 작곡한 16음 펜타토닉 루프를 비트마다 신스 연주 | 메트로놈→음악. 타이밍 직관·몰입↑. 외부 AI 아닌 음표 데이터 직접 작곡, 배열로 확장 | src/systems/sfx.ts, src/scenes/rhythm.ts |
| 07-27 | (버그수정) 홀드 노트가 유지 중 사라짐 — 컬링을 시작 시각→끝 시각 기준으로 | 홀드 길이(0.67~1.33s)가 컬 임계(0.3s)를 넘겨 유지 중 삭제됨. 렌더만 수정 | src/scenes/rhythm.ts |
| 07-27 | **리듬 현란 연출**: 변속·웨이브 궤도(판정선엔 정확히 수렴) + juice(섬광·파티클·shake) | 시각 혼란·눈요기 요구. 경로만 왜곡, 판정선 수렴이라 공정(귀=멜로디로 맞춤). 순수 렌더 | src/scenes/rhythm.ts |
| 07-27 | (수정) 리듬 x변속 롤백 → 등속 + 웨이브는 1/3 노트만 | x변속이 도착을 실제보다 일찍 보이게 해 온타이밍이 early로 무판정("눌러도 실패"). 모든 노트 굽이침도 과함 | src/scenes/rhythm.ts |
| 07-27 | **리듬 스테이지 진행**: 정확도(%) 표시 + 90%↑ 시 다음 스테이지(bpm↑·판정창↓·동시/홀드↑) | 반복 리플레이·난이도 곡선. 정확도=perfect1/good0.6 가중. 종료 진행은 pointer/key(harness가 play만 update) | src/systems/rhythm.ts, src/scenes/rhythm.ts |
| 07-27 | **리듬 노트 기믹**: 협동 홀드(전원 같이 길게 눌렀다 다같이 떼기) + 연타(한 노트 여러 번) | 사용자 요청. "여러명이 같이" + "연타". 릴레이(번갈아)는 판정 복잡해 보류 | src/systems/rhythm.ts, src/scenes/rhythm.ts |
| 07-27 | (밸런스) 연타 완화: 필요 5→3~4회, 창 1→1.6박, 앞뒤 grace 추가 | ~7.5회/초 요구가 과함 → ~2~2.7회/초로 | src/systems/rhythm.ts |
| 07-29 | `Result` 에 `mash` 추가. 연타 진행 중 판정을 `hold` 와 분리 | 플레이테스트에서 연타 노트에 "홀드를 유지해주세요"가 떴다. 연타는 `hold` 필드를 창 길이로 재사용하는데 result 까지 공유해 UI가 홀드로 오인 | src/systems/rhythm.ts, src/scenes/rhythm.ts |
| 07-29 | 개인 비교를 점수 → **적중률**(`pacc/pjudged`)로 교체. `winner()` 도 적중률 기준 | 변칙 차트는 담당이 랜덤이라 P1/P2 노트 수·종류가 다르다. 연타·홀드 보너스 배분까지 운이라 점수 비교는 실력이 아닌 차트 운을 쟀다 (사용자 지적) | src/systems/rhythm.ts, src/scenes/rhythm.ts |
| 07-29 | 동시류를 개인 적중률에 **포함**. 팀이 깨지면 `did` 로 귀책자만 실패 처리 | 처음엔 "팀 책임"이라 제외했으나 오판. 동시 탭 미press·협동홀드 조기 뗌은 `syncPressed`/`player` 로 귀책이 특정된다 (사용자 지적) | src/systems/rhythm.ts |
| 07-29 | (검증) 연타 난이도 플레이테스트 **합격, 스테이지 4까지** — 07-27 완화 확정, 추가 튜닝 안 함 | 사람이 직접 쳐보고 "칠만해". 126bpm에서도 요구 ~4~5.3회/초로 여유. 테스트는 규칙대로 동작함만 증명했고 칠 만한지는 증명 못 했었다 | evidence/playtest-2026-07-29-mash.md |
| 07-29 | 리드인 카운트다운에도 첫 노트 담당 표시 (아래 키 힌트 강조 + "첫 노트는 P1 — [F]") | 아래 표시가 `clock >= beatTime(0)` 이후에만 강조돼 리드인 2초 내내 양쪽이 회색. 노트는 이미 다가오는데 담당을 몰라 첫 판정을 놓친다 (플레이테스트 발견) | src/scenes/rhythm.ts |
| 07-29 | **네온 시그니처 채택**: 핑크 #FF2E97 → 사이언 #00E5FF, 보랏빛 흑 #0A0612. 판정선은 담당 플레이어 색 글로우, 그라데이션은 장식 전용 | "디자인이 구리다·시그니처 컬러가 없다" 지적. bass-platform 에 트렌드 디자인 정의가 없음을 전수 확인 후 직접 수립. 그라데이션 끝이 P1 색과 같아 상태 요소에 쓰면 원칙 1이 깨짐 | src/ui/tokens.ts, src/scenes/*, DESIGN.md, index.html |
| 07-29 | design critic high 1·medium 3건 해소 (hover P1색 / 정확도 모수 / rgba 리터럴 7곳 / P3↔success 충돌) | `bass critique stop` 이 stop:false 였음. 재검증에서 stop:true — 남은 건 low·note 5건 | critiques/TASK-002/design-2.yaml |
| 07-29 | `DESIGN.md` + `src/ui/tokens.ts` 신설, `bass.yaml` 에 `web` 프로파일 추가 | 디자인 규칙이 어디에도 정의돼 있지 않았다. 조사 결과 배경 4종·보조텍스트 7종·성공 3종·실패 3종·폰트 17단계가 혼재. BASS design critic 과 `bass design check` 도 web 프로파일에서만 켜진다 | DESIGN.md, src/ui/tokens.ts, src/scenes/*, index.html, bass.yaml |
| 07-29 | 홀드 꼬리를 궤도 따라 물결로. `wavy(k)` 에 홀드 항상 포함(탭은 1/3 유지) | 꼬리를 머리·끝 두 점 직선으로 그려 웨이브 노트에서도 1자였다. 홀드의 2/3은 애초에 직선 궤도였다 (사용자 지적) | src/scenes/rhythm.ts |
| 07-29 | 리듬 모드를 하위 메뉴로 분리 (게임 선택 → 팀전/개인전) | 최상위에 리듬 항목이 둘이라 목록이 늘어짐. `MenuScene` 에 title/hint 만 추가해 하위 메뉴로 재사용 — 새 씬 클래스 없음 | src/main.ts, src/scenes/menu.ts |
| 07-29 | 판정 등급별 피드백 차등 (`feedback()` 한 곳으로 통합) | perfect/good 이 파티클까지 동일해 구분이 안 됐다. 링 겹수와 붉은 가장자리는 한 프레임만 봐도 갈린다. 통합하면서 step 경로의 홀드 완주에 흔들림이 빠져 있던 것도 함께 해소 | src/scenes/rhythm.ts |
| 07-30 | **반전 노트** — 노트 일부가 상대 색으로 오지만 담당자가 눌러야 한다. `Note.swap` 은 표시 색만 바꾼다 | 목적은 난이도가 아니라 쇼츠각. 지금 게임에서 둘이 서로를 의식하는 순간이 협동 홀드 브레이크 하나뿐이고, 개인전은 동시류가 없어 상호작용이 0이었다. 반전은 개인전에도 붙어 첫 구멍을 낸다. 표시만 반전시켜서 `press`·`tally`·`winner` 를 안 건드렸다 — 책임을 반전시키면 적중률이 엉뚱한 사람에게 붙는다 | src/systems/rhythm.ts, src/scenes/rhythm.ts |
| 07-30 | 반전을 **DESIGN.md 원칙 1(색은 담당을 말한다)의 유일한 예외**로 명문화. 판정선·노트 글·차례 안내 세 신호는 사실 유지 | 색만 속이고 판단 정보는 전부 준다. 반사는 색을 따라가므로 난이도는 유지되고 "몰라서 틀렸다"는 사라진다. 원칙을 조용히 깨지 않는다 | DESIGN.md |
| 07-30 | 마우스 기믹·보이다 안 보이는 노트는 **채택 안 함**(반전만 채택) | 마우스는 각자 마우스·각자 화면(네트워크)이 전제라 지금 만들면 2인 플레이테스트가 불가능하고 시점 분리 후 두 번 만들게 된다. 보이다 안 보임은 플레이어만 힘들고 시청자가 난이도를 읽지 못해 쇼츠각이 안 된다 (사용자와 함께 판단) | tasks/TASK-006.md |
| 07-30 | 반전 표식을 노트 안 `🔀P2` 글자 → **담당 색 점선 링**(rad+9, 파선 5/4)으로 이동 | 플레이테스트 "반대로 눌러야 하는 노트 가시성이 좋지 않다". 12px 이모지가 반지름 18~22 원 안에서 뭉개졌고, 노트 모양이 일반 노트와 같아 한눈에 튀는 게 없었다. 표식을 글자에 걸었던 게 원인 — DESIGN.md 원칙 2대로 모양으로 옮겼다. 몸통은 상대 색 유지라 반사 트랩은 그대로 | src/scenes/rhythm.ts, DESIGN.md |
| 07-30 | **플레이어당 키 2개(레인 4)** — P1 D/F, P2 J/K. `Note.lane` + `press(player, lane)`, 담당과 키가 둘 다 맞아야 판정 | 실력 축이 타이밍 하나뿐이었다. 키를 늘리면 "언제"에 "어디로"가 더해지고 반전과 곱해진다(상대 색 노트를 내 다른 키로). `keysPerPlayer` 미지정 시 lane 0 + rnd() 미소비라 기존 차트·테스트 보존 | src/systems/rhythm.ts, src/scenes/rhythm.ts |
| 07-30 | `holdLane` 추가 — 홀드 중 같은 사람의 **다른 키** keyup 은 무시 | 키가 둘이 되면 D 를 누른 채 F 를 떼는 일이 생긴다. lane 을 안 보면 홀드가 조용히 끊겨 미스가 된다. `release` 한 곳에 막아 모든 호출부가 함께 고쳐진다 | src/systems/rhythm.ts |
| 07-30 | 동시류는 **레인을 따지지 않는다** (자기 키 아무거나) | 전원이 함께 누르는 노트에 "상대의 몇 번째 키"는 의미가 없다. lane 검사를 owner>=0 분기에만 둬서 추가 코드 없이 성립 | src/systems/rhythm.ts |
| 07-30 | 웨이브 진폭 26 → **6**. 제약 `WAVE + RAD <= LANE_H/2` 명문화 | 레인이 4줄이 되면서 ±26 웨이브가 옆 레인을 침범해 "어느 키인가"를 못 읽게 만들었다. TASK-005 물결의 의도적 후퇴 — 레인 판독이 굽이침보다 먼저다 | src/scenes/rhythm.ts |
| 07-30 | 노트 라벨 `P1` → **키 글자**(D/F/J/K), 판정 팝업을 판정선 왼쪽·판정 레인 높이로 이동 | P1 이 키를 둘 가지면 "P1" 은 정보가 아니다. 팝업은 하이웨이가 4레인으로 커지면서 노트 위에 겹칠 위치가 됐다 — x < HIT_X 는 노트가 구조적으로 없는 구역이라 원칙 5를 좌표로 보장한다 | src/scenes/rhythm.ts, DESIGN.md |
| 07-30 | 반전 노트가 **상대 레인에서 등장해 담당 레인으로 건너온다**(`laneAt(k,p)` 보간). 색만 반전시키던 것을 위치까지 확장 | 플레이테스트 "라인으로 오니까 그 사람이 누르면 그냥 맞게 되는 거 아니야?" — 정확한 지적. 레인 도입(TASK-007) 후 위치가 색보다 강한 신호가 됐고, 담당 레인에 그리니 위치가 진실을 말해 색 거짓말이 무의미해졌다. 끝까지 남의 줄에 두면 표적 원과 어긋나 버그처럼 보이므로 판정 시점에 수렴시켰다 | src/scenes/rhythm.ts, DESIGN.md |
| 07-30 | 플레이어당 키 2개 → **3개**(레인 6줄). P1 `S D F` / P2 `J K L` — 홈 포지션 세 손가락 | 사용자 요청 '키 하나씩 더 늘려줘'. 기존 D/F·J/K 를 유지하고 바깥으로 하나씩 추가해 근육 기억을 보존했다 | src/scenes/rhythm.ts |
| 07-30 | 레인 기하를 상수 → **유도식**으로. `LANE_H`·`RAD`·`WAVE`·힌트 박스 폭을 `LANES` 에서 계산 | 키를 2→3 으로 늘리는데 하이웨이가 y 422 까지 내려가 키 힌트(y 400)와 겹칠 뻔했다. 값 네 개를 매번 손으로 맞추면 반드시 하나를 놓친다 — 제약(`WAVE + RAD <= LANE_H/2`, 점선링 `rad+6` 도 레인 안)을 주석이 아니라 식으로 옮겼다 | src/scenes/rhythm.ts |
| 07-30 | **새 게임 주제 "모방" 채택** (CON-002, 87/100). 선정 기준은 재미가 아니라 *쇼츠를 대량으로 만들 수 있는가* | 후보 4개(모방·통역·폭탄 돌리기·유령)를 (1)범인이 사람으로 특정되고 매 판 바뀌는가 (2)자막·소리 없이 2초에 읽히는가 (3)게임이 비교 가능한 산출물을 스스로 뱉는가 로 걸렀다. 모방은 원본/P1/P2 삼단 이미지가 곧 클립이라 "대량"이 구조적으로 성립한다. 전염·전력망은 이미 했고, 폭탄 해체는 매뉴얼이 대량 수작업 콘텐츠라 게이트 7 탈락 | nan/concepts/CON-002.yaml |
| 07-30 | 작업 ID 계열을 **`MIM-###`** 로 분리 (`TASK-###` 은 하네스·리듬 계열) | 다른 게임이라 번호를 섞으면 이력이 뒤엉킨다. bass 태스크 ID 패턴이 `^[A-Z][A-Z0-9]*-\d+$` 라 접두사를 자유롭게 딸 수 있다 (사용자 지적) | tasks/MIM-001.md |
| 07-30 | 열화를 **중계에만** 걸었다. P1 자기 캔버스는 사라지지 않는다 | 둘 다 사라지게 하면 원본↔P1 이 P1 의 실력이 아니라 기억력이 되어 층 구분이 무너진다. 중계만 열화시키면 전달(원본↔P1)과 채널 손실(P1↔P2)이 각각 의미를 갖고 범인이 갈린다 | src/scenes/mimicry.ts |
| 07-30 | 점수를 3개(전달·재현·총점) 내고 **낮은 쪽을 붉게** 표시 | 총점만 보여주면 점수는 나오지만 누가 망쳤는지 안 갈린다. 이 저장소 최대 교훈 — 숫자는 아무도 못 읽고 "네가 망쳤다"는 읽힌다 | src/scenes/mimicry.ts |
| 07-30 | 유사도는 32×24 격자 IoU + **1칸 팽창**. 순수 IoU 아님 | 얇은 선끼리는 한 칸만 어긋나도 교집합이 0이 되어 사람 눈의 "비슷한데"와 크게 벌어진다. 팽창 1회로 고정하고 가혹하면 거리변환으로 바꾼다. 이 판정이 납득되는지가 MIM-001 의 존재 이유다 | src/systems/mimicry.ts |
| 07-30 | 원본을 **덧그릴 수 없게** 별도 참조 상자로 분리. 참조·그리기 칸의 비를 4:3 으로 통일 | 겹쳐 두면 P1 이 베껴서 원본↔P1 이 항상 100% 가 되고 중간 층이 무의미해진다. 비가 다르면 정규화 좌표가 눌려 잘 그려도 점수가 깎인다 | src/scenes/mimicry.ts |
| 07-30 | 세로 조각을 **2분할 한 화면 · 순차**(P1→P2)로. 정보 비대칭 미성립을 알려진 한계로 명시 | 마우스가 하나라 동시 플레이가 물리적으로 불가능하다. 네트워크를 세로 조각에 넣으면 6시간을 넘긴다 — 판정·연출·유사도를 먼저 검증하고 비대칭은 네트워크에서 온다 (사용자 결정) | tasks/MIM-001.md |
| 07-30 | `kits/grid` 를 재사용하지 않았다 | IoU 는 칸 집합의 교/합집합이고 Grid 는 값 맵이라 set/has 만 쓰게 된다. `Set<number>(r*cols+c)` 가 더 짧다. `kits/dragdrop` 도 개별 아이템 이동용이라 자유 곡선 캡처에 맞지 않는다 | src/systems/mimicry.ts |
| 07-30 | 모방(CON-002/MIM-001) **취소.** 게이트는 다 통과했으나 재미없었다 | 원인 셋: (1) 페이로드가 절차 생성 폴리라인이라 의미가 없었다 — 텔레폰 드로잉이 웃긴 건 뜻이 변하기 때문이고 추상 낙서가 다른 추상 낙서로 바뀌는 건 아무것도 위반하지 않는다. 게이트 7(대량 수작업 콘텐츠)을 과잉 적용해 단어 목록 100개를 배제한 판단이 웃음을 없앴다 (2) 페이오프를 총점 %로 만들어 "정확도 87.3%는 아무도 못 읽는다"는 자기 교훈을 반복했다 (3) 14초가 운동 과제였고 순간의 결정이 없었다. 코드는 사용자 지시로 삭제하지 않고 둔다 | records/MIM-001.json |
| 07-30 | **새 주제 "떠넘기기" 채택** (CON-003, 88/100). 주사위 랜덤성 | 사용자 요청. 랜덤 전제: 순수 랜덤은 클립이 안 된다(동전 던지기를 아무도 안 클립한다) — 굴리기 **전에** 사람이 뭔가를 걸어야 punchline 이 생긴다. 랜덤이 주는 것은 (1)반응이 순수해진다 (2)맥락 없이 긴장이 읽힌다 (3)변량이 곧 다양성이다. 가장 귀한 조합은 "랜덤이면서 범인이 사람" | nan/concepts/CON-003.yaml |
| 07-30 | 붕괴 방어 2종: **차례에 한 번 굴려야 넘길 수 있다** + **오른 게이지만큼 내 pot 도 오른다** | 넘기기가 공짜면 "항상 즉시 넘기기"가, 굴리기가 순수 손해면 "아무도 안 굴린다"가 지배전략이 된다. 둘이 맞물려야 "굴릴까 넘길까"가 실제 결정이 된다. 첫 규칙 덕에 넘기기가 회피가 아니라 공격이 된다 | src/systems/pushluck.ts |
| 07-30 | **생명을 두지 않는다.** 목표 점수 단독 승리 조건 | 생명으로 지면 점수가 장식이 되고, 그러면 "점수 차이가 정답을 바꾼다"는 깊이의 유일한 출처가 사라진다. 상태도 줄어든다 | src/systems/pushluck.ts |
| 07-30 | **pot 은 라운드 끝에만 확보.** 터진 쪽 소멸 + 상대가 자기 pot 확보 | 넘길 때 확보하면 넘기기가 지배전략에 가까워지고 라운드 전체가 걸린 대치가 안 된다. 의도한 부수 효과 — pot 큰 쪽이 위험 회피적이 되고 적은 쪽이 자유롭게 굴린다. 굴리면 입장이 뒤바뀌어 자기 교정된다 | src/systems/pushluck.ts |
| 07-30 | **임계값은 범위만 공개.** 폭발 확률은 표시하지 않는다 | 완전 미지는 계산 불가라 체념이 되고 완전 공개는 산수가 된다. 확률까지 띄우면 도박이 스프레드시트가 된다. 범위 공개가 시청자에게도 위험을 읽히게 한다 | src/scenes/pushluck.ts |
| 07-30 | **범인 3종(passer/self/none)을 이름으로 적는다** | 페이오프가 숫자면 클립이 안 된다는 모방의 실패를 되풀이하지 않는다. 자책(self)과 비난(passer) 두 종류가 번갈아 나서 클립이 단조로워지지 않는다. 판정은 굴리기 전에 잡아야 한다 — rolled 가 곧 바뀐다 | src/systems/pushluck.ts |
| 07-30 | `winner()` 반환형을 `number` 로 명시 | 리터럴 유니온(-1\|0\|1)으로 추론되면 호출부에서 `C.player[w]` 튜플 인덱싱이 TS2514 로 막힌다 | src/systems/pushluck.ts |
| 07-30 | 떠넘기기 게이지를 **가로 막대 → 타들어가는 심지**로. 규칙·수치 무변경 | 사용자 요청(겉모습 교체). 후보 3개(심지·물컵·젠가) 중 심지 — 게임 원형이 폭탄 돌리기라 주제와 맞고, 불꽃 위치=게이지·붉은 구간=임계범위가 1:1로 떨어져 억지가 없다. 물컵은 눈금 두 개가 어색하고 젠가는 물리가 필요해 6h 위험 | src/scenes/pushluck.ts |
| 07-30 | 폭발을 **심지가 끊긴 자리**에서 낸다. 끝의 폭탄은 도달하지 않는 게 정상 | 폭탄에서 터지면 "끝까지 가야 터진다"로 읽혀 숨은 임계값 규칙과 어긋난다. `newRound` 가 게이지를 0 으로 되돌리므로 씬이 터진 게이지를 따로 기억해야 했다 — 안 그러면 폭발이 출발점에서 난다 | src/scenes/pushluck.ts |
| 07-30 | 심지 물결 진폭을 5 로 제한, 불꽃 지글거림은 파티클 없이 반지름 흔들림으로 | 진폭이 크면 불꽃이 어디쯤인지 흐려져 장식이 판정을 가린다(DESIGN 원칙 5). 파티클 배열·수명 관리를 더하면 "렌더만 바꾸는 작업"이 아니게 된다 | src/scenes/pushluck.ts |
| 07-30 | 떠넘기기에 **라운드 규칙 9종 + 판돈 상승** 추가. 조작은 2종 그대로 | 사용자 지적 "너무 심플해서 컨텐츠가 많이 안 나올 것 같다" — 맞다. 변주가 결과(숫자)에만 있고 상황에 없어서 40번째 판이 1번째 판과 화면이 같았다. "매판 다른 클립"이 사실은 "매판 다른 숫자"였다. 동사를 늘리면 판독성이 무너지므로 상황만 바꾼다 — 화면 위 한 줄이 그대로 클립 자막이 된다 | src/systems/pushluck.ts, src/scenes/pushluck.ts |
| 07-30 | 1라운드는 항상 기본, 2라운드부터 추첨(`none` 포함) | 첫 판부터 변칙이면 무엇이 기본인지 배울 자리가 없다. `none` 을 풀에 남기는 이유는 평범한 판도 나와야 특별한 판이 특별해지기 때문이다 | src/systems/pushluck.ts |
| 07-30 | 되돌리기는 **터진 라운드를 통째로** 되돌린다. RNG 는 되감지 않는다 | 살아남은 굴림을 취소하는 건 후회할 일이 없어 드라마가 안 난다 — "터졌는데 살아났다"가 이 규칙의 전부다. RNG 까지 되감으면 같은 눈이 또 나와 되돌리기가 무의미해진다 | src/systems/pushluck.ts |
| 07-30 | 되돌리기만 **세 번째 버튼**을 허용. 그 라운드에만·1회만 노출 | "조작 2종 이하" 하드게이트와 부딪히는 것은 사실이다. 상시 조작이 아니라 조건부 능력이라 취지는 지킨다고 판단했고, 숨기지 않고 기록한다 | src/scenes/pushluck.ts |
| 07-30 | 두 배는 **점수에만** 걸고 게이지는 그대로 / 뜨거운 감자 +5 는 터짐을 판정하지 않음 | 두 배가 게이지까지면 짧은 심지와 같은 규칙이 된다. 아무도 안 굴렸는데 터지면 이상하므로 +5 는 다음 굴림을 확정 사망으로 만들 뿐이고 책임은 넘긴 사람에게 간다 | src/systems/pushluck.ts |
| 07-30 | 테스트 기본 `modPool: ["none"]` | 규칙 추첨이 기존 14개 판정을 흔들면 무엇이 깨졌는지 알 수 없다. 규칙별 검증은 `withMod` 로 하나씩 고정해서 따로 한다 | src/pushluck.test.ts |
| 07-30 | 되돌리기를 라운드당 공용 1회 → **각자 1회**로. 스냅샷이 자기 시점 충전량을 담아 유효성을 스스로 판정 | 플레이테스트 "아이템이 공용인데?" — 먼저 쓰는 사람이 상대의 구조 수단을 보이지 않게 빼앗았다. 경쟁 게임에서 그런 비대칭은 전략이 아니라 불공평이다. 고치면서 '충전이 라운드 경계를 넘어 남는다'는 별개 한계도 같이 사라졌다 — 원인이 같았고, 유효성을 스냅샷에 맡기니 캐리오버 해킹이 통째로 필요 없어졌다 | src/systems/pushluck.ts |
| 07-30 | 라운드 규칙 `nopass`(넘기기 금지) **폐기**. 9종 → 8종 | 플레이테스트 "너무 불합리하다" — 맞다. 그 라운드는 결정이 아예 없다. 터질 때까지 굴리는 것 말고 할 게 없고 상대는 가만히 앉아 pot 을 확보한다. 게임이 있어야 할 자리에 게임이 없었다. PSH-003 사양에 "빼는 것도 성과"로 적어둔 항목이 실제로 발동했다 | src/systems/pushluck.ts |
| 07-30 | **새 주제 "제로섬" 채택** (CON-004, 89/100). 자리를 뺏는 방식 | 사용자가 주제를 정하고 세 방향(점수·자리·시간) 중 자리를 골랐다. 칸 수가 고정이라 총합이 저절로 0이 되어 규칙을 따로 안 만들어도 된다. 제로섬이 공짜로 주는 둘 — (1)범인이 구조적으로 항상 명확하다(내가 잃은 1칸 = 네가 가져간 1칸), 지금까지 게임마다 따로 붙이던 것이 규칙에 내장돼 있다 (2)막대 하나가 곧 판세다 | nan/concepts/CON-004.yaml |
| 07-30 | **처음부터 꽉 찬 판**(70:70). 빈 칸을 두지 않는다 | 빈 판에서 시작하면 초반이 제로섬이 아니다(빈 칸은 누구 것도 아니다). 꽉 채워두면 1턴부터 모든 수가 순수한 강탈이고, 총합 불변이라 막대 하나로 판세를 표현할 수 있다 | src/systems/zerosum.ts |
| 07-30 | **매 턴 끝에 자기 칸 수만큼 점수.** 최종 판만 세지 않는다 | 최종 판만 세면 마지막에 두는 사람이 반격을 안 받아 유리하고, 서로 되받아치면 초반 수가 무의미해져 반경 운으로 결판난다. 매 턴 파이를 나누면 둘 다 막히고, 주제가 오히려 더 순수해진다 — 매 수 양쪽 점수 합이 정확히 140이다 | src/systems/zerosum.ts |
| 07-30 | 반경을 **맨해튼 거리(다이아몬드)** 로. 클릭 **전에** 공개 | 체비쇼프(정사각)는 r=3 이 49칸으로 판의 35%라 한 수에 결판난다. 다이아몬드는 25칸(18%). 반경을 뒤에 알려주면 결정이 사라지고 그냥 운이 된다 | src/systems/zerosum.ts |
| 07-30 | `kits/grid` 를 또 재사용하지 않았다 | `Map` 기반 희소 저장이라 140칸이 전부 차 있는 판에는 배열이 맞다. 좌표 변환만 필요한데 그건 곱셈 두 번이다. 온도·전염 제거 이후 네 게임 연속 실사용자가 없다 — 삭제 후보 근거가 또 하나 늘었다 | src/systems/zerosum.ts |
| 07-30 | (버그) 메뉴 행 기하를 상수 → **항목 수 유도식**으로. 세로 중앙 정렬 | 5번째 게임(제로섬)이 y 494 에 그려져 캔버스(480) 밖으로 나갔다 — 히트박스도 같은 좌표라 **마우스로 접근조차 못 하는 상태**였다(사용자 발견, 숫자키로만 진입 가능했다). 리허설4 레인에서 같은 병을 겪고 '서로 물린 기하 값은 하나에서 유도한다'를 교훈으로 적어놓고 여기 적용을 안 했다. 1~7항목까지 안전하고 하한(34px)과 천장을 `ponytail:` 주석으로 남겼다 | src/scenes/menu.ts |
| 07-30 | 제로섬 뺏는 범위를 **반경 1종 → 모양 7종**(십자·막대·기둥·네모·별·큰십자·고리) | 플레이테스트 "모양이 너무 단순하게 하나인 게 별로다". 반경만 1~3으로 바뀌니 매 수가 같은 그림이었다 — 떠넘기기에서 진단한 "변주가 결과에만 있고 상황에는 없다"가 재현됐다. 반경 산식을 오프셋 테이블로 바꿔 계산 지점 하나(`area`)만 교체했다 | src/systems/zerosum.ts |
| 07-30 | **한 라운드는 양쪽이 같은 모양을 쓴다** (라운드 = P1 한 수 + P2 한 수) | 플레이테스트 "칸 자체는 P1·P2 같아야 될 것 같은데". 맞다 — 제로섬은 파이가 고정이라 운의 비대칭이 그대로 승패가 되고 만회할 여지가 규칙에 없다. ZRO-001 기록에 "반경 운이 결과를 지배하는가"를 미검증으로 적어뒀던 항목을 사용자가 플레이 전에 구조로 짚었다. 이제 운은 "무슨 모양이 오나"에만 남고 실력은 "어디 놓나"로 온전히 간다 | src/systems/zerosum.ts |
| 07-30 | 모양은 **전부 점대칭**만 쓴다. 비대칭(L·T·S) 배제 | 비대칭 모양을 넣으면 회전 조작이 필요해지고 조작 1종(클릭)이 깨진다. 회전이 없으면 방향 운도 안 생긴다. 테스트가 점대칭을 못 박는다 | src/systems/zerosum.ts |
| 07-30 | 뺏는 크기가 깎인 **이유를 화면에 적는다** — `최대 16칸 · 판 밖 −9 · 내 칸 −7` | 플레이테스트 "잡아먹는 칸의 크기가 다 다른데 무슨 기준이야?" — 화면이 모양만 말하고 (1)가장자리 잘림 (2)이미 내 칸인 낭비분을 안 말했다. 셋이 겹쳐 크기가 달라지는데 하나만 보여주니 제멋대로로 보였다 | src/scenes/zerosum.ts |
| 07-30 | 제로섬 종료 화면에 **판세 궤적 그래프** + 최고의 한 수 | 판이 끝나면 어떻게 이겼는지가 안 남았다. 제로섬은 총합이 고정이라 판세가 선 하나로 완벽히 그려지는데 그걸 안 쓰고 있었다 — 다른 게임은 총합이 안 고정이라 이런 선이 아예 안 나온다. P1 칸 수 하나만 그린다(P2 는 여집합이라 선 두 개는 같은 정보를 두 번 그리는 것) | src/systems/zerosum.ts, src/scenes/zerosum.ts |
| 07-30 | 제로섬 **시작 배치 5종**(좌우·상하·사분·가로줄·세로줄). 전부 정확히 70:70 + 뒤집기 대등 | 항상 좌우로 시작하니 첫 몇 수가 늘 같은 최적수였다. 산식으로 딱 떨어지는 다섯만 골랐다 — 정렬해서 앞 70개를 자르면 무늬가 깨진다. 가운데/바깥 배치는 중앙이 구조적으로 유리해 대등성을 못 지켜 뺐다 | src/systems/zerosum.ts |
| 07-30 | (버그) 시드 LCG 에 **워밍업 16회** 추가 — 첫 출력이 이웃 시드끼리 거의 같았다 | 배치 추첨 테스트가 잡았다: 시드 1~40 이 전부 같은 배치를 줬다. `1013904223 + seed*1664525` 를 2^32 로 나누면 0.236~0.252 라 5로 곱해도 항상 1이다. 완화지 해결이 아니다 — 근본은 LCG 초기 상관이고 제대로 고치려면 시드 혼합 함수가 필요하다. **rhythm·mimicry·pushluck 도 같은 병이다**(그쪽은 앱이 큰 난수 시드를 써서 안 드러날 뿐) | src/systems/zerosum.ts |
| 07-30 | 시드 난수를 **`src/kits/rng.ts` 로 뽑고 splitmix32 로 교체**. 게임 5종의 인라인 LCG 5개 제거 | 같은 LCG 가 다섯 파일에 복사돼 있었고 결함이 두 개 있었다 — 둘 다 복사본 수만큼 존재했다. (1)이웃 시드끼리 첫 출력이 거의 같아 시드 1~200 이 **전부 같은 값**을 줬다(제로섬 배치 테스트가 잡음) (2)`0xffffffff` 로 나누면 정확히 1.0 이 나와 `Math.floor(rnd()*n)` 이 `n` 을 돌려줬다 — 리듬은 `gaps[5]`→`undefined`→비트 시각 전부 `NaN`, 담당이 `players`가 되어 배열 범위 밖. 확률 2^-32 라 본 적은 없지만 실재. splitmix32 는 시드를 섞어 통과시키므로 워밍업이 불필요하고 `2^32` 로 나눠 `[0,1)` 이 보장된다 | src/kits/rng.ts + systems 5개 |
| 07-30 | 제로섬의 LCG 워밍업 16회와 `ponytail:` 주석 제거 | 완화가 필요 없어졌다. 워밍업은 상수를 눈으로 골라야 했고(8회면 0,2,0,2 주기가 남았다) 근본 원인을 안 고친 것이었다 | src/systems/zerosum.ts |
| 07-30 | 난수 교체로 깨진 테스트 1건 — 차트 모양 의존을 제거 | `keysPerPlayer 미지정` 테스트가 0번 노트를 탭으로 가정하고 `perfect` 를 기대했는데, 새 수열에서 그 자리가 연타가 되어 `mash` 가 났다. **엔진은 정상**이고 테스트가 차트 모양에 묶여 있었다 — 평범한 탭 노트를 찾아서 검사하도록 고쳤다 | src/rhythm.test.ts |
| 07-30 | (버그) 겹치는 글자 3곳 수정 + `src/kits/layout.ts` 신설 | 플레이 지적 "글자가 겹쳐서 못 읽겠다". 좌표를 뽑아 세로 구간을 계산하니 실재 겹침 셋 — 제로섬 요약이 격자 위로 4.4px 올라탐 · 제로섬 이유 줄이 화면 밖 0.4px · 떠넘기기 차례 줄이 배너를 7.6px 파고듦. **원인이 매번 같다: `fillText` 의 y 는 baseline 이라 글자가 위로 `0.8×크기` 만큼 뻗는데 그걸 안 뺐다.** 세션 내내 "계산으로 배치하고 화면은 안 봤다"로 기록된 것의 실제 청구서다 | src/kits/layout.ts, scenes 2개 |
| 07-30 | 제로섬 미리보기 요약을 **두 줄 → 한 줄**로 합침 | 격자 하단(456)과 캔버스(480) 사이가 24px 뿐이라 두 줄이 물리적으로 안 들어간다. 격자를 올리면 판세 막대와 부딪힌다 | src/scenes/zerosum.ts |
| 07-30 | 떠넘기기 세로 재배치 — 배너 `56,h26`→`54,h22` · 차례 줄 `92`→`96` · 주사위 `138`→`146` | 배너만 옮기면 점수판과 겹치고 차례 줄만 내리면 주사위와 겹친다 — 셋을 같이 움직여야 했다. 넘기기 거절 문구는 좌표 대신 `!this.boom` 조건으로 막았다(터진 순간에 같이 뜰 이유가 없다) | src/scenes/pushluck.ts |
| 07-30 | 겹침 검사 테스트를 **가로 정렬을 고려해** 작성 | 처음엔 세로만 보고 한 줄로 세웠더니 제목(x 16~96, 왼쪽 정렬)과 점수판(x 200)이 "겹친다"고 나왔다 — 실제로는 안 겹친다. 가운데 열만 스택으로 묶고 제목은 가로 분리를 따로 검사한다 | src/layout.test.ts |
| 07-30 | (버그) HUD 를 `position: fixed` → **문서 흐름 안**으로. 캔버스와 같은 폭·정렬 + 높이 3줄 고정 | 스크린샷으로 확인 — HUD 글자가 캔버스 안 제목("떠넘기기")과 점수판("P2 0")을 덮고 있었다. `fixed` 라 자리를 안 차지해 캔버스가 페이지 맨 위에서 시작했고, 폭 제한이 없어 가운데 정렬된 캔버스를 가로질렀다. **캔버스 안쪽은 TASK-010 으로 이미 멀쩡했다 — 남은 겹침은 전부 캔버스 밖이었다.** min-height 는 HUD 길이가 게임·상태마다 달라 캔버스가 세로로 튀는 것을 막는다 | index.html, DESIGN.md |
| 07-30 | (버그) 모방 화면 세로 재배치 — 라벨 3곳 겹침 해소, **차례 안내를 오른쪽 빈 공간으로** | 스크린샷 확인. `panel()` 이 라벨을 상자 바로 위(`b.y−6`)에 그리는데 원본 상자(48~168)와 그리기 칸(180~) 사이가 12px 뿐이라 라벨·차례 안내가 전부 부딪혔다. 그런데 **오른쪽 위(x 200~624, y 56~168)가 통째로 비어 있었다** — 좁은 틈에서 자리를 다투는 대신 비어 있는 곳을 썼다. 상자 위 17px 여백을 규칙으로 두고(F.sm 이 baseline 위로 11.2px), 4:3 비는 유사도 공정성의 전제라 절대 안 깼다 | src/scenes/mimicry.ts |
| 07-30 | (버그) 제로섬 — 모양 글리프를 **차례 줄 오른쪽**으로 빼고 판세 막대 `100`→`92` | 스크린샷 확인: 첫 수 안내가 판세 막대 **위에 얹혀** 있었다(5.2px). 위쪽에 다섯 층(차례 줄·글리프·점수 라벨·막대·안내)이 쌓였는데 격자가 136 에서 시작해 여유가 없었다. 글리프는 7px 단위라 고리(5×5)가 35px — 세로에서 빼니 나머지가 다 숨을 쉰다. 모방(TASK-012)과 같은 수다: **세로가 모자라면 가로를 본다** | src/scenes/zerosum.ts |
| 07-30 | (버그) 전력망 — 도시 블록을 통째로 10px 아래로(`py = my+42` → `my+52`), 이벤트 배너 `+30`→`+44` | `🏙️ 도시 전력 공급` 라벨이 용량선을 7.6px 파고들었다. **용량선이 막대 위아래로 4px 씩 튀어나온다**(`my-4 ~ my+mh+4`)는 것을 계산에 안 넣었다 — 기준이 막대 두께가 아니라 장식이었다. 라벨만 내리면 아이콘과, 아이콘만 내리면 배너와 부딪혀 `py` 하나로 셋을 같이 옮겼다 | src/scenes/power.ts |
| 07-31 | 주제 **"코너킥"** 채택 (CON-005, 승인 78/100) — 이 저장소 첫 실시간 자유이동 게임 | 사용자가 주제와 역할 분담(한 명이 올리고 한 명이 넣는다, 수비수·GK 는 AI)을 지정했다. 실패한 코너킥 뒤에 키커와 스트라이커가 서로를 탓하는 것은 축구의 실제 풍경이라 **범인 다툼이 주제에 내장돼 있다** — 게임마다 따로 만들어 붙이던 것이 또 공짜로 나온다. 점수 78 은 CON-004(89)보다 낮고 feasibility 15·visualClarity 11 에서 깎였다. 그럼에도 만드는 이유는 punchline(골망)이 가장 명확하고 novelty(6, 최저)가 아니라 구조가 강점이기 때문이다 | nan/concepts/CON-005.yaml |
| 07-31 | **파워 정확도가 낙하 원의 초기 반경을 정한다** — `R0 = R_MIN + (1-정확도)×(R_MAX-R_MIN)` | 이 한 줄이 셋을 동시에 푼다. (1)정보 비대칭 없이 긴장이 생긴다 — 헤더가 조준을 봐도 실제 낙하점은 원 안 어딘가라 핫시트에서 화면을 가릴 필요가 없다 (2)범인이 기계적으로 정해진다 — 원이 컸으면 공 탓, 작았으면 머리 탓 (3)랜덤(흩어짐)과 결정(어디로)이 분리된다. 떠넘기기·제로섬이 쓴 구조와 같다 | src/systems/corner.ts |
| 07-31 | **낙하 원의 중심이 조준점에서 실제 낙하점으로 미끄러진다.** 반경은 0으로 준다 | 중심을 처음부터 진실에 두면 첫 프레임에 답이 보이고 "포착"이라는 동사가 사라진다. 부수 효과로 **낙하점이 매 시점의 원 안에 항상 들어 있다** — 원이 거짓말을 하지 않는다는 것이 산식에서 따라 나오고 테스트가 못 박는다. 흩어짐이 판 밖으로 나가면 잘라내지 않고 **오프셋을 줄여서** 들인다(잘라내면 원 밖으로 나가 불변식이 깨진다) | src/systems/corner.ts |
| 07-31 | **수비 AI 는 헤더가 아니라 원의 중심을 쫓는다** | 축구가 그렇다 — 공중볼에서 수비수도 낙하점을 본다. 그리고 헤더를 쫓게 하면 AI 가 영원히 따라다녀 게임이 술래잡기가 된다. 같은 목표점을 향해 달리는 경주여야 "반 발 먼저"가 성립한다 | src/systems/corner.ts |
| 07-31 | 수비수·GK 에 **반응 지연**(0.35s / 0.85s). 수비수는 코너 시작 시 **헤더를 마크하고 선다** | 지연 없이 짜니 둘 다 벽이었다 — 수비수는 1.7초에 230px 를 가서 낙하점에 늘 먼저 닿고, GK 는 존 안을 100% 먹어 아무도 안 노리는 죽은 구역이 됐다. 지연을 주면 사거리가 182px·102px 로 줄어 뚫릴 여지가 생긴다. 수비수를 고정 위치에 두는 안은 "깊게 올리면 수비수가 못 온다"가 정답이 돼 버려 폐기했다 | src/systems/corner.ts |
| 07-31 | **GK 커버 폭이 거리에 비례해 넓어진다** — `GK_SPAN × (1 + 거리/220)`, 골문 폭이 상한 | 없이 짜니 **깊게 올리는 것이 정답**이었다. 수비수도 GK 도 못 오는 뒤쪽이 공짜라 딜레마가 통째로 사라졌다. 거리로 커버를 넓히면 "안전한 곳은 골도 안 된다"가 되어 골문 앞(GK 도박) / 중거리(완벽한 점프) / 깊은 곳(안전하지만 무득점)의 삼분 구도가 선다. 조준 중에 골라인 위에 그려 클릭 전에 보이게 했다 | src/systems/corner.ts, src/scenes/corner.ts |
| 07-31 | 실패 사유를 **6종으로**. `골키퍼가 나왔다` 와 `골키퍼 선방` 을 안 합친다 | 컨셉은 5종이었다. 둘은 정반대 상황이다 — 하나는 GK 가 존으로 뛰어나와 걷어낸 것이고 하나는 GK 가 골문을 지키고 있었고 슛이 그 코스로 간 것이다. 한 이름으로 합치면 다음에 뭘 고쳐야 하는지가 사라진다. 6종이 **키커 3 · 헤더 3** 으로 갈리고 테스트가 그 균형을 확인한다 | src/systems/corner.ts |
| 07-31 | 높이를 **그림자 하나가 아니라 단서 셋**으로 표현 — 그림자 · 잇는 점선 · 커지는 공 | 평면에 3D 를 그리는 첫 사례고 CON-005 가 "판독되면 우아하고 안 되면 게임이 안 읽힌다"를 최대 미지수로 적었다. 단서 하나에 걸지 않고 셋을 겹쳤다 — 셋 다 같은 정보를 말하므로 하나가 안 읽혀도 나머지가 받는다 | src/scenes/corner.ts |
| 07-31 | 필드 높이를 380 → **360** 으로 줄여 아래 60px 을 게이지·상태줄에 준다 | 480 까지 늘리면 파워 게이지가 경기장 위에 올라탄다. 이 저장소가 네 번 낸 겹침 버그(TASK-010~013)를 다섯 번째로 내지 않으려면 **자리를 먼저 비워두고 그려야** 한다 — 그리고 나서 옮기는 것이 지금까지의 패턴이었다 | src/systems/corner.ts |
| 07-31 | 헤더는 **파워 단계부터 움직일 수 있다** | CON-005 가 "키커가 조준하는 동안 P2 가 할 일이 없다"를 위험으로 적었다. 헤더를 미리 움직이게 하면 그 시간이 준비 시간이 되고, 조준점을 보고 미리 붙을 수 있지만 실제 낙하점은 다르므로 공짜 이득도 아니다 | src/systems/corner.ts |
| 07-31 | (버그) 코너킥 경합을 **최종 거리 → 도착 시각** 비교로. 수비수 판정 반경 30 → 26(헤더와 동일) | 플레이 지적 "헤더가 수비수를 절대 못 이기는 판정인 것 같은데?" — 맞았다. 400판 시뮬레이션으로 재니 **완벽하게 움직이는 헤더 상대로도 수비수가 55%** 를 먹었다. 원인 둘: (1)`step()` 이 목표점에 정확히 붙어 수비수는 `defDist = 0` 이 되는데 사람은 근사값이라 거리 비교에서 질 수가 없다 (2)수비수 판정만 30px 로 헐거워, 둘 다 원 중심을 쫓으면 수비수의 도착이 기하학적으로 먼저 참이 된다. **명세에는 처음부터 "반 발 먼저"라고 써놓고 구현만 거리 비교였다** | src/systems/corner.ts |
| 07-31 | 판정 순서 변경 — 헤더가 아예 못 붙은 경우(`공이 나빴다`·`늦게 붙었다`)를 `수비가 먼저 봤다` 보다 먼저 본다 | 도착 시각으로 바꾸면서 같이 잡았다. 헤더가 200px 밖에 있는데 "수비가 먼저 봤다"로 적으면 왜 못 붙었는지가 사라진다 — 거기서는 수비수가 있었든 없었든 결과가 같았다 | src/systems/corner.ts |
| 07-31 | 수비수 속도를 헤더보다 느리게(135) → **같게(150)**, 지연 0.35 → 0.25 | 거리 비교를 고치니 이번엔 반대로 수비수 승률이 **2%** 로 떨어져 장식이 됐다. 더 느린데 지연까지 있으면 같은 목표를 쫓을 때 헤더가 구조적으로 늘 먼저 도착한다 — 경주가 아니라 정해진 결과인 것은 마찬가지였다. 속도를 맞추니 15% 가 되어 시작 기하와 달린 궤적이 승부를 가른다 | src/systems/corner.ts |
| 07-31 | 밸런스를 **시뮬레이션 테스트로** 못 박는다 (수비수 승률 5~30%, 골 30~70%) | 이 버그는 단위 테스트 39개를 전부 통과한 채로 존재했다 — 각 분기가 도달 가능한지만 봤지 **어느 분기가 실제로 얼마나 나오는지**는 아무도 안 봤다. 이상적 헤더를 400판 돌리는 검사 하나가 그것을 잡는다. 한계도 명시: 최적 추적 AI 라 R0(불확실성)의 무게는 안 잡힌다 | src/corner.test.ts |
| 07-31 | 수비수 1명 → **4명(지역 방어 3 + 전담 마크 1)**. 가장 가까운 한 명만 공에 나간다 | 사용자 요청. 넷을 다 헤더에게 붙이면 어디로 올려도 같은 상황이 되고, 넷이 다 공으로 달려들면 경주가 아니라 포위다(이상적 헤더 상대 수비 45% · 골 23%). 실제 코너킥처럼 지역에 흩어 세우니 **어디로 올리느냐가 누구를 상대하느냐를 정하는 결정**이 새로 생겼다 — 수비가 하나였을 때는 없던 것이다 | src/systems/corner.ts |
| 07-31 | (버그) **서 있는 수비는 도착으로 치지 않는다** — 공을 읽기 전(`DEF_DELAY`)은 경합이 아니다 | 4인 지역 방어가 박스를 덮으니 그 근처에 떨어지는 공은 t=0 에 이미 경합 안이라 진 공이 됐다. 가만히 서 있던 것은 경합에서 이긴 게 아니다 — **뛰어든 헤더가 서 있던 수비를 이기는 것이 공중볼 경합**이다. 이 한 줄이 인과 사슬을 완성했다: 정확한 크로스만이 헤더에게 미리 붙을 시간을 준다 | src/systems/corner.ts |
| 07-31 | (측정 오류) 밸런스 시뮬레이션에 **헤더의 파워 단계 사전 이동**을 넣었다 | 수비 4인이 너무 세게 나와 노브를 만지려던 참에, 시뮬레이션의 "완벽한 헤더"가 정작 **이 게임에서 헤더의 가장 큰 무기를 안 쓰고 있었다**는 것을 발견했다. 실제 수단을 반영 안 한 측정치에 맞춰 밸런스를 깎을 뻔했다 — 고치니 노브를 하나도 안 건드리고 수치가 제자리를 찾았다 | src/corner.test.ts |
| 07-31 | 수비 4인이 **인과 사슬을 살렸다** — 골이 파워에 따라 56% → 47% → 40% 로 단조 감소 | 수비 1명일 때는 완벽 파워와 최악 파워의 골 차이가 6% 뿐이라 게임의 전제("키커의 정확도가 헤더의 난이도")가 사실상 죽어 있었다. 지역 수비가 박스를 덮자 부정확한 크로스가 처음으로 대가를 치른다. **수비를 늘린 것이 밸런스 문제가 아니라 설계 해법이었다** | src/systems/corner.ts, src/corner.test.ts |
| 07-31 | 주제 **"총알피하기"** 채택 (CON-006, 승인 88/100). 원안(1인 생존)에서 **2인 + 색 요격**으로 | 원안은 하드게이트 4(주제를 빼면 규칙이 달라지는가)에 정면으로 걸렸다 — 총알을 돌·운석으로 바꿔도 규칙이 한 줄도 안 바뀌는 **장르명**이었고, 1인이라 범인 다툼이 없고, 생존 시간이 숫자 페이오프였다(MIM-001 이 취소된 병). 엄폐(첫 몸 정지) 안을 거쳐 사용자가 직접 제안한 색 규칙으로 왔다. playability 24 는 이 저장소 최고치다 | nan/concepts/CON-006.yaml |
| 07-31 | **총알에 색이 있고 자기 색은 자기를 못 죽인다.** 내 색이 상대에게 갈 때 내가 몸으로 막는다 | 사용자 제안. 이 한 줄이 셋을 동시에 푼다 — (1)협동이 규칙에서 나온다(엄폐안은 상대를 방패로 쓰는 배신이었는데 이건 살리러 간다) (2)**회피와 요격이 같은 조작(이동) 안에서 갈려** 조작을 안 늘리고 동사를 하나 더 얻는다 (3)"그거 네 색이었잖아"는 변명이 안 되는 범인 지적이다 | src/systems/dodge.ts |
| 07-31 | **색 탄은 항상 반대 색 플레이어를 조준한다** (불변식, 테스트가 못 박음) | 자기 색을 조준하면 그냥 흡수하면 그만이라 규칙이 통째로 죽는다. 상대를 노려야 내가 달려갈 이유가 생긴다 | src/systems/dodge.ts |
| 07-31 | **주인 없는 탄은 두 사람의 중점을 조준한다** — 무작위 한 명이 아니라 | 컨셉이 딜레마 표에 "붙어 있으면 한 발에 둘 다 걸린다"를 적어놓고 **구현을 안 했다.** 무작위 조준이면 붙어 있기가 공짜가 되고, 조준점이 늘 코앞이라 요격도 공짜가 된다. 중점을 노리니 붙어 있기 48% / 떨어지기 25% 로 주인 없는 탄 사망 비중이 갈렸다 — **적어둔 딜레마가 그제야 코드에 생겼다** | src/systems/dodge.ts |
| 07-31 | (측정 오류) 요격률 97%를 보고 세 번 헛발질했다 — **지표가 틀렸다** | "이상적 헤더/플레이어" 시뮬레이션으로 재니 색 탄 요격률이 97%였다. 예고·탄속·조준 산포를 차례로 만졌는데 96~98%에서 안 움직였다. 원인은 밸런스가 아니라 지표였다 — `blocked` 는 **애초에 빗나갈 총알까지 흡수한 것**을 다 센다(짝이 이미 움직였으므로). 죽음의 구성으로 바꿔 재니 판당 4.1명 사망 · 색탄 69% · 주인없는탄 31% 로 멀쩡했다. **노브에 반응하지 않는 지표는 지표가 틀린 것이다** | src/dodge.test.ts |
| 07-31 | 조준 산포(`AIM_SPREAD`)를 넣었다가 **지웠다** | 요격률을 낮추려고 넣었는데 빗나감만 늘려 생존이 55s → 90s 로 오히려 쉬워졌다. 값을 못 했으므로 삭제 — 잘못된 지표를 쫓아 더한 복잡도였다 | src/systems/dodge.ts |
| 07-31 | 밸런스 딜레마를 **회귀 테스트로** 못 박는다 (간격 3종 × 30판) | 코너킥에서 배운 것을 적용했다 — 분기가 도달 가능한 것과 분기가 실제로 얼마나 나오는지는 다르다. 여기서는 한 발 더 나가 **"붙어 있으면 주인 없는 탄이, 떨어지면 색 탄이 더 죽인다"** 는 딜레마 자체를 검사로 만들었다. 한쪽 거리가 무료 정답이 되면 이 테스트가 깨진다 | src/dodge.test.ts |
| 07-31 | 총알을 **색 + 모양**으로 구분한다 (원·네모·가시 마름모) | DESIGN.md 원칙 2. 이 게임은 색이 곧 생사라 색맹 대응이 선택이 아니다. 동시에 총알 색이 곧 주인이므로 원칙 1(플레이어 색은 그 사람을 가리킬 때만)에 정확히 맞는다 — **플레이어 색이 장식이나 상태가 아니라 규칙 자체가 되는 첫 사례다** | src/scenes/dodge.ts |
| 07-31 | 동시 실시간 2인이라 키를 갈랐다 — P1 WASD · P2 방향키 | 코너킥은 단계(파워/비행)가 갈려 스페이스를 공유할 수 있었지만 여기는 둘이 계속 동시에 움직인다. 한 키보드에서 손이 부딪히는지는 플레이해야 안다 | src/scenes/dodge.ts |
| 07-31 | (버그) 메뉴 행 안 글자를 **비율 배치 → 스택 배치**로. 행 높이 하한 34 → 40(`BLOCK_H + 7`) | 배포 화면 지적 "글자들이 칸을 약간 넘어서 나오고 있어". 7항목에서 행이 36px 이 되자 이름(band 1.8~19.8)과 설명(18.5~30.5)이 **1.32px 겹쳤다.** 원인은 `0.45/0.62/0.78` 비율 — 폰트 크기는 고정인데 행만 줄어드니 세 줄이 서로 밀린다. 이름·설명을 한 덩어리로 묶어 행 안 세로 중앙에 놓고, 번호는 `textBaseline="middle"` 로 바꿔 비율 계산을 아예 없앴다. **하한이 처음으로 근거를 갖는다** — 예전 34 는 그냥 고른 값이었다 | src/scenes/menu.ts |
| 07-31 | 자리를 만들려고 밴드를 넓히고 간격을 줄였다 — `LIST_TOP` 140→132 · `LIST_BOT` 470→476 · `GAP` 12→8 | 폰트를 줄이는 대안이 있었지만 설명이 이미 F.xs(12)라 시연 화면에서 안 읽힌다. 판독성이 최우선이므로(DESIGN.md Purpose) 공간을 넓히는 쪽을 골랐다. 힌트 아래끝이 120.4 라 132 는 11.6px 여유가 있다 | src/scenes/menu.ts |
| 07-31 | (테스트) 메뉴 검사를 `h*0.78 + 4` 에서 **`kits/layout` 의 `checkStack`** 으로 | **이 버그가 통과된 이유가 테스트에 있었다** — baseline 에 손으로 고른 4 를 더해 봤을 뿐이고, 이름과 설명이 서로 겹치는지는 아예 검사 대상이 아니었다. `textBand`·`checkStack` 이 정확히 이걸 위해 있는데 메뉴에는 안 쓰고 있었다. **baseline 을 band 로 안 본 다섯 번째 사례다**(TASK-010~013). 옛 비율을 그대로 넣어 새 검사가 겹침을 잡는 것까지 확인했다 | src/menu.test.ts |
| 07-31 | 메뉴 천장 7항목을 **테스트로 못 박는다** — 8항목은 캔버스를 넘는다 | `n*40 + (n-1)*8 ≤ 344` 를 풀면 7이다. TASK-008 이 주석으로만 적어둔 천장이 근거 없는 값이었고 실제로 7에서 깨졌다. 이제 `fits(8) === false` 를 검사해 **조용히 잘리는 대신 테스트가 먼저 깨진다** | src/menu.test.ts |
| 07-31 | 떠넘기기 굴림을 **3단 연출**로 늦춘다 (구름 0.85s → 확정 0.35s → 심지가 탄다) | 플레이 지적 "주사위 굴러가는 이펙트 등 좀 천천히 인터페이스적으로 긴장감 유도". 지금까지는 누르면 눈·게이지·폭발이 **한 프레임에** 확정됐다 — 이 게임의 punchline 이 "언제 끊길지 모르는 심지"인데 **끊기는 과정이 화면에 없었다.** PSH-002 가 심지를 그림으로 만들고도 그 그림에 시간을 안 준 채였다 | src/scenes/pushluck.ts |
| 07-31 | **결과를 먼저 확정하고 화면만 늦춘다** — 엔진은 한 줄도 안 건드린다 | 연출 도중에 엔진이 판정하는 방식은 되돌리기·시드 결정성과 충돌한다. 누르는 순간 `roll()` 이 끝나고 `Anim` 이 그 결과를 3단으로 풀어놓을 뿐이라, **엔진 테스트 31개가 한 줄도 안 바뀌고 통과한다**(무변경의 증거로 체크섬을 확인했다) | src/scenes/pushluck.ts |
| 07-31 | 심지가 타는 시간을 **눈에 비례**시킨다 (`PER_PIP` 0.17s, 하한 0.25s) | 고정 시간이면 1 과 6 이 같은 긴장이 된다. 비례시키면 6이 나올 때 1초 넘게 불꽃이 붉은 구간을 향해 기어가서 **긴장의 크기가 위험의 크기를 따라간다** — 규칙을 새로 만들지 않고 이미 있는 값에서 나온다 | src/scenes/pushluck.ts |
| 07-31 | 연출 중 **모든 입력을 막고** 판 종료도 연출 뒤로 미룬다 | 안 막으면 같은 굴림이 두 번 들어가거나 터지기 전에 넘겨진다. 그리고 `Harness` 는 `phase === "play"` 에서만 `update` 를 돌리므로, 마지막 굴림에서 `to("win")` 을 먼저 하면 **연출이 그 자리에서 멈춘다** | src/scenes/pushluck.ts |
| 07-31 | (버그) 터짐 연출 중 심지가 출발점으로 돌아가 있었다 — 끊긴 자리에 세워둔다 | `newRound` 가 엔진 게이지를 0 으로 되돌려서, 폭발은 끊긴 자리에 그려지는데 재와 불꽃만 앞으로 가 있었다. 즉시 판정일 때는 눈에 안 띄었지만 기어가는 연출을 넣으니 드러났다 — **연출을 넣으면 그 전에는 안 보이던 모순이 보인다** | src/scenes/pushluck.ts |
| 07-31 | 굴림 연출 시간을 **약 60% 로 단축** (눈 6 기준 2.22s → 1.35s) | 플레이 지적 "너무 길어 조금 빠르게". 긴장을 만들려던 시간이 기다림이 됐다. 네 노브(`TUMBLE`·`SETTLE`·`PER_PIP`·`CREEP_MIN`)를 같은 비율로 줄였다 — `PER_PIP` 만 줄이면 큰 눈의 긴장이 사라지고 `TUMBLE` 만 줄이면 굴러가는 느낌이 사라진다. **비례는 유지된다** | src/scenes/pushluck.ts |
| 07-31 | (버그) 주사위가 게이지 숫자를 파고들었다 — 주사위 `r 34→30`·`y 146→142`, 팝 `+0.35→+0.22`, 게이지 숫자 `198→210` | 스크린샷 지적. **평상시 4.4px · 팝일 때 16.3px** 겹쳤고 차례 줄과도 0.3px 스쳤다. **PSH-004 가 만든 게 아니라 원래 있던 겹침**인데, 연출이 길어져 팝이 큰 상태로 오래 떠 있게 되자 드러났다. 위(차례 줄)와 아래(심지)가 막혀 있어 주사위가 줄어드는 것이 유일하게 빈 자리였다 | src/scenes/pushluck.ts |
| 07-31 | (테스트) 떠넘기기 겹침 검사에 **게이지 숫자를 넣고 주사위를 최대 팝으로** 잰다 | **검사가 이걸 놓친 이유가 더 중요하다.** 스택에 게이지 숫자가 아예 없었고, 주사위는 `146 - 34` 처럼 손으로 적은 **평상시** 기하였다 — 소스를 따라가지도, 최대 크기를 보지도 않았다. 이제 `DIE`·`POP`·`GAUGE_Y` 를 export 해 소스에서 가져오고 `DIE.r * (1 + POP)` 로 잰다. 옛 기하를 새 검사에 넣어 겹침이 잡히는 것을 확인했다. **`kits/layout` 을 만들고도 적용 범위가 좁아 생긴 여섯 번째 사례다**(TASK-010~013, TASK-014) | src/layout.test.ts |
| 07-31 | 뜨거운 감자 넘기기 비용 **5 → 2** (`HOT_POTATO_COST`) | 플레이 지적 "중간에 순서 넘길때 5늘어나는건 값이 너무 큰거같아". 수치로 재니 맞았다 — 5는 6면 주사위 평균 굴림(3.5)의 **143%** 라 **게임 이름이 "떠넘기기"인데 넘기는 것이 굴리는 것보다 비쌌다.** 게이지 상승의 59%가 넘기기에서 나와 주사위가 부차적이 됐고(굴림 연출로 긴장을 만들어놓고 정작 그 눈이 덜 중요해진다) 라운드가 3.8차례로 반토막 나 pot 이 쌓이기 전에 터졌다. 2면 넘기기 몫 36%·라운드 5.9차례다. 1은 규칙이 있는지 모를 만큼 약하고 3은 넘기기 몫이 절반에 가까워 다시 주사위를 밀어낸다 | src/systems/pushluck.ts |
| 07-31 | 불변식을 못 박는다 — **넘기기 비용 < 평균 굴림** | 값이 아니라 **관계**를 검사한다. 이 게임의 핵심 동사가 넘기기이므로 넘기는 것이 굴리는 것보다 비싸면 안 된다. 주사위 면 수가 바뀌어도 따라오고, "손으로 고른 상수는 그 상수가 틀리면 조용히 통과한다"(TASK-014·PSH-005)를 세 번째로 반복하지 않는다 | src/pushluck.test.ts |
| 07-31 | (테스트) `passer` 귀책 검사가 **비용 크기에 기대고 있었다** — 게이지를 직접 세우도록 고침 | `limitMin/Max 6` 에 "+5면 무조건 임계를 넘는다"로 짜여 있어서, 비용을 2로 낮추자 **조용히 깨졌다.** 이제 `gauge = 임계 - 비용 + 1` 로 세워 비용이 얼마든 의도(넘겨두면 다음 굴림이 넘긴 사람 탓)만 검사한다. **밸런스 값에 의존하는 검사는 밸런스를 못 바꾸게 만든다** | src/pushluck.test.ts |
| 08-06 | 총알피하기를 **3D 로 전환**한다 (CON-007, 승인 90/100 — 이 저장소 최고치). 카메라 3안 중 **기울인 아레나** | 사용자 요청. 어깨 뒤 3인칭은 **기각했다** — 막을 대상과 그쪽으로 가는 탄이 동시에 보여야 "내 색을 내가 막는다"가 성립하는데 시야 밖으로 나간다. 화면을 쪼개면 "그거 네 색이었잖아"가 못 봤으니까 성립하지 않는다. **카메라 문제가 아니라 컨셉 문제라 닫았다.** 고도 있는 탄(점프·숙이기)은 3D 여야만 되는 유일한 규칙이지만, 요격이 지금도 최대 미지수라 예고 시간 하나에 걸린 문제에 높이 축을 얹으면 무엇이 망가졌는지 못 가린다 — 뺐다 | nan/concepts/CON-007.yaml |
| 08-06 | 세계는 **부서지는 발판**. 아레나가 원 하나에서 **부채꼴 조각 14개 × 단계 4개** 배열이 된다 | 세계관 4안(압착 사격장·홀로 테이블·부서지는 발판·종이 디오라마) 중 사용자 선택. 고른 기준은 **"아레나가 왜 좁아지는가"** 다 — 지금은 원이 그냥 작아지고 아무도 이유를 모른다. 넷 중 셋은 렌더만 바꾸면 됐고 **발판만 엔진을 연다**(조각 경계가 새 판정이다). 대신 좁아짐이 연속이 아니라 **사건**이 되고, 조각마다 반지름이 따로 놀아 판이 울퉁불퉁해진다 | nan/concepts/CON-007.yaml |
| 08-06 | **막으면 땅이 돌아온다** — 요격 N회마다 **마지막으로 막은 자리**의 조각이 한 단계 솟는다 | 사용자 제안. 지금까지 요격의 보상은 "상대가 안 죽는다"는 **손실 회피뿐**이었다 — 잘 막아도 판은 똑같이 좁아지고 화면에 남는 것이 없었다. 조각이 돌아오면 요격에 양의 보상이 생기고 **협동이 공간이라는 자원으로 계량된다.** 어느 조각이 돌아오는지가 이 규칙의 전부다: 무작위면 "가끔 판이 넓어진다"가 되고, **막은 자리**여야 몸을 던진 그 자리에서 땅이 솟는 한 프레임이 나온다 | src/systems/dodge.ts (DGE-002) |
| 08-06 | 불변식을 못 박는다 — **판당 복구 단계 수 < 판당 붕괴 단계 수** | 복구가 붕괴를 이기면 판이 안 끝나고 후반 압박이 통째로 사라진다. 산술로는 안전하다(붕괴 2초/단계면 본전에 초당 0.5회 요격이 필요한데 요격 기회 자체가 초당 0.33회를 못 넘는다) — **복구는 지연이지 역전이 아니다.** 그래도 값이 아니라 관계를 검사한다. 붕괴 주기나 게이지 임계를 나중에 바꿔도 "안 끝나는 게임"이 조용히 들어올 수 없다 (PSH-006 패턴) | src/dodge.test.ts (DGE-002) |
| 08-06 | `RESTORE_BLOCKS` 를 **손으로 고르지 않고 측정에서 유도**한다 | 판당 `blocked` 절대 횟수를 30판 × 간격 3종으로 재고 복구/붕괴 비율이 0.3~0.6 이 되는 값을 고른다. **`blocked` 는 애초에 빗나갈 총알까지 다 센다**(DEC-DGE-METRIC-WRONG)는 것을 알고 재야 한다 — 요격률이 아니라 절대 횟수를 본다. 이 저장소는 손으로 고른 상수로 세 번 데었다(TASK-014·PSH-005·PSH-006). 네 번째를 만들지 않는다 | tasks/DGE-002.md |
| 08-06 | 조각을 **무작위로** 무너뜨린다 — 가장 바깥 단계부터가 아니라 | 바깥 단계부터 고르면 판이 균일하게 내려가 **그냥 작아지는 원**이 되고 조각으로 나눈 이유가 사라진다. 무작위여야 톱니가 생기고, 톱니가 생겨야 **판 모양이 그 판의 기록**이 된다 — CON-006 이 쇼츠각 2번에 스스로 적어둔 함정("변주가 결과에만 있고 상황에는 없다")이 여기서 풀린다 | src/systems/dodge.ts (DGE-002) |
| 08-06 | 사람이 **원통 → 우주복 작업자**. 목숨 3단계가 **실루엣**이다 | 사용자 지적 "그냥 긴 원통형이라서 몰입이 잘 안돼". 원인은 모양만이 아니었다 — **세 번 맞을 때까지 몸이 안 변했다.** 생명이 머리 위 점 3개에 적혀 있고 시선은 총알에 가 있으니 아무도 안 본다. 3 온전 / 2 그을음·바이저 금 / 1 붉은 점멸·공기 샘·절뚝으로 갈랐다. **상대가 급한 것이 보여야 "막으러 갈지"가 판단이 된다** | tasks/DGE-003.md |
| 08-06 | 헬멧을 **둥근(P1) / 각진(P2)** 으로 가른다 — 총알의 원·네모와 **같은 축** | 캐릭터 모양은 취향이 아니라 이미 절반 정해져 있었다. 총알이 원·네모·가시 마름모다(DEC-DGE-SHAPE-TOO). 사람이 같은 대비를 쓰면 **"저 탄은 내 거"가 색 없이도 읽히고** 색맹 대응이 덤이 아니라 규칙에서 나온다. 방패처럼 앞뒤가 뚜렷한 형태는 **쓰지 않는다** — 판정이 원이라 "앞으로 막는다"는 거짓말이 된다 | tasks/DGE-003.md |
| 08-06 | **흰 우주복을 못 쓴다** — 우주복은 그 사람 색의 어둡고 탁한 버전이다 (`C.suit`) | 가장 사람다운 선택이 흰색인데 어두운 배경에서 **흰 덩어리가 총알과 밝기로 경쟁한다.** 이 게임은 색이 곧 판정이라 그 경쟁에서 지는 쪽이 규칙이다. 같은 색상·다른 밝기로 두면 소속은 즉시 읽히고 총알과는 안 싸운다. 판·선·배경은 기존 토큰을 재사용하고 **새 색은 우주복 6개뿐**이다(원칙 6) | DESIGN.md, src/ui/tokens.ts (DGE-003) |
| 08-06 | 새 원칙 — **밝기가 곧 판정 관여도** (화면 4층) | 색을 어디에 얼마나 쓸지가 그때그때 결정되면 반드시 총알과 경쟁한다. 층 1 총알·예고선(발광·채도 최대) / 층 2 바이저·어깨 띠(좁게 발광) / 층 3 우주복 본체(어두운 파생) / 층 4 발판·관절(배경 톤). **사람 몸에 흰색은 없다.** 이 규칙은 이 게임 밖에도 쓰이므로 DESIGN.md 원칙으로 올린다 | DESIGN.md |
| 08-06 | 3D 는 **거리 오판을 만든다** — 바닥 예고선·발밑 그림자·판정 링 셋으로 갚는다 | 요격은 거리를 재는 동사라 깊이 착시와 정면으로 부딪힌다. **예고선을 공중이 아니라 바닥에 긋는다** — 공중의 선은 어디에 꽂히는지 안 읽히고, 바닥이면 착탄점이 그림자와 같은 평면에 있어 거리가 그대로 보인다. 사람 발밑 판정 링은 **항상** 그린다. 그리고 **몸통 폭 = 판정 지름을 코드가 강제한다**(투영된 `K.BODY` 에서 배율을 뽑는다) — 실루엣이 판정보다 크면 "안 맞았는데 맞았다"가 되고 요격도 같은 방식으로 거짓말이 된다 | tasks/DGE-003.md |
| 08-06 | 2D 씬과 3D 씬을 **병행할 수 없다 — 교체한다** | TASK-008 이 메뉴 7항목 천장을 테스트로 못 박았고(`fits(8) === false`) 총알피하기가 7번째다. 3D 를 별도 항목으로 추가하면 **그 테스트가 깨진다.** `src/scenes/dodge.ts` 는 지우지 않고 남긴다 — 3D 가 요격을 망가뜨리면 되돌릴 자리다 | src/scenes/menu.ts (DGE-003) |
| 08-06 | 작업을 **엔진(DGE-002) → 렌더(DGE-003)** 로 가른다. 규칙을 2D 에서 먼저 검증한다 | 복구가 재미있는지는 렌더와 무관하고 2D 화면이 싸다. **복구가 재미없으면 3D 씬을 만들기 전에 알아야 한다.** CON-007 의 feasibility 14 는 이 저장소 최저치인데(3D·조각·게이지·캐릭터가 한 번에 든다) 둘로 가르면 각각이 되돌릴 수 있는 크기가 된다 | tasks/DGE-002.md, tasks/DGE-003.md |
| 08-06 | **딜레마 표(60/130/220px)를 재측정해야 한다** — 깨질 것이 확실하다 | CON-006 의 회귀 테스트가 "붙어 있으면 주인 없는 탄이, 떨어지면 색 탄이 더 죽인다"였다. **판이 원이 아니게 되면 이 값이 그대로일 이유가 없다.** 톱니 때문에 한쪽 거리가 무료 정답이 되면 게임이 정적이 되므로, 새 값을 재서 GAME_SPEC 과 테스트 양쪽에 다시 박는다. 검사를 지우는 것이 아니라 **다시 측정하는 것**이다 | src/dodge.test.ts (DGE-002) |
| 07-27 | (작업 규칙) 이후 게임 변경은 GAME_SPEC/DECISIONS에 항상 반영 | 사용자 요청 — 증거 하네스 최신 유지 | — |

## 폐기 아이디어 — 리허설2 "전염" (20개 생성 → 6개 통과)

6축 조합에서 시작(장르명 금지). 게이트 하나라도 실패 시 폐기.

| 아이디어 (공간·동사·시스템·압박·주제·보상) | 폐기 사유 |
|---|---|
| Network·Connect·Replicate·HiddenInfo·Contagion·Reveal — 숨은 감염 노드를 추론해 격리선 연결 | G5 대표장면 약함(추론=정적) + G3 6h 초과(그래프 UI) |
| Queue·Negotiate·Learn·ConflictingGoals·Reciprocity·MassMovement — 감염자 줄 협상 배치 | G1 한문장 불가, G2 조작 3종↑ |
| Stack·Merge·Accumulate·Overload·Dependency·Collapse — 감염 층 쌓아 붕괴 유도 | G4 주제제거해도 성립(그냥 스택붕괴) |
| Territory·Allocate·Compete·Scarcity·Sacrifice·Reversal — 구역 포기해 감염 희생 | G6 신규시스템 3개(영역판정+AI경쟁+감염) |
| Flow·Route·Drift·DelayedConsequence·Boundary·Compression — 감염류 우회 배관 | G3 6h 초과(유체 라우팅) |
| Arena·Imitate·Chase·Instability·Perspective·NearMiss — 감염 추격 회피 | G4 주제제거해도 성립(순수 추격) |
| Lane·Switch·Oscillate·RuleMutation·Ambiguity·Explosion — 레인 전환 감염 폭발 | G7 대량 레인 콘텐츠 필요 |
| Grid·Predict·ChainReact·Irreversibility·Memory·Restoration — 과거 감염경로 기억 복원 | G6 신규(예측판정+기억버퍼+감염) 3개 |
| Grid·Cut·Spread·Time·Contagion·ChainReaction — **시간차 확산을 장벽으로 절단해 성역 보호** | ✅ 통과 → 채점 88 (채택) |
| (나머지 통과 5개: Place/Grid 변주 — 예산·시드수·성역위치만 다름. 최상위와 중복도 높아 축소 후 폐기, 채점 70~78) | 상위안에 흡수 |

## 하드 게이트 (문서 8) — 하나라도 실패 시 폐기

1. 한 문장으로 설명 가능한가
2. 핵심 조작 2종 이하인가
3. 6시간 안에 세로 조각 가능한가
4. 주제를 빼면 규칙이 달라지는가
5. 대표 장면(5초)을 설명할 수 있는가
6. 신규 핵심 시스템 3개 미만인가 (목표 최대 2)
7. 대량 수작업 콘텐츠가 불필요한가
